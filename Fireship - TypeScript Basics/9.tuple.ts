// TUPLES (Not present in vanilla JavaScript/ECMAScript)
// Can only contain numbers, strings and booleans.
type myList = [number?, string?, boolean?]      // ? makes a dataype optional

var list : myList = []
list.push(2)
list.push('two')
list.push(true)
// list.push(['arr'])  // Error

